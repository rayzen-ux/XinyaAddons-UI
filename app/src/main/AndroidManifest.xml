<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:label="Port Extras"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.Rianixia">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Addons"
            android:icon="@mipmap/ic_launcher">

            <intent-filter>
                <action android:name="android.settings.APPLICATION_DEVELOPMENT_SETTINGS" />
                <action android:name="com.android.settings.action.SETTINGS" />
                <action android:name="com.android.settings.action.EXTRA_SETTINGS" />
                <action android:name="com.android.settings.action.IA_SETTINGS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <meta-data
                android:name="com.android.settings.category"
                android:value="com.oplus.settings.category.ia.more_system_settings" />

            <meta-data
                android:name="com.android.settings.summary"
                android:value="Addons Utilities" />

            <meta-data android:name="com.android.settings.order" android:value="-41" />

        </activity>

    </application>
</manifest>